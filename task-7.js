// Write a JavaScript exercise to get the extension of a filename
function findExtension(file) {
  filename = file.split(".").pop();
  // console.log(typeof(filename))
  console.log(filename);
}

findExtension("task-7.js");
